{
  "name": "real-time-communication-with-socket-io",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:open": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client -- --open\"",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "env:setup": "node ./scripts/create-env.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
