# Client example environment variables for Vite
# Copy this file to `.env` in the `client/` folder and customize values as needed

# Where the client should connect for socket.io
VITE_SOCKET_URL=http://localhost:5000
