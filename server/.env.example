# Server example environment variables
# Copy this file to `.env` in the `server/` folder and customize values as needed

# Port the Express server listens on
PORT=5000

# URL of the client app (used for CORS configuration in server)
CLIENT_URL=http://localhost:5173
